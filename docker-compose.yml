services:
  vizer:
    image: ${DOCKER_USERNAME:-worxbend}/twitch-vizer:latest
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      VIZER_SERVER_HOST: "0.0.0.0"
      VIZER_SERVER_PORT: "8080"
    volumes:
      - ./data:/data
    restart: unless-stopped
